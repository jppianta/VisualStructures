class LL {
    Node {
        number x,
        Node next
    }
    
    Node root  = Node(2);
    
    fun add():void {
        number n = 20;
        Node aux = root;
        while (aux.next != null) {
            aux = aux.next;
        }
        aux.next = Node(n);
    }
}